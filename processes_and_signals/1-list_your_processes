#!/usr/bin/env bash
# Display all currently running processes for all users, including those without a TTY,
# in a user-friendly format, and show process hierarchy.

ps -e --forest -o user:15,pid:8,%cpu:5,%mem:5,vsz:10,rss:10,tty:8,stat:6,start:10,time:10,command

# Explanation:
# - The `:N` after each field specifies the width (in characters) for that column.
#   For example, `user:15` reserves 15 characters for the 'USER' column.

